### Backend API configuration (adjust values as needed)
# Vite exposes only variables prefixed with VITE_ to the client code.

# Development API endpoint (updated to 8081, no /api suffix per backend spec)
VITE_API_URL_DEV=http://localhost:8081

# Staging API endpoint
VITE_API_URL_STAGING=https://staging.api.example.com

# Production API endpoint
VITE_API_URL_PROD=https://api.example.com

# Generic fallback (used if specific env not set)
VITE_API_URL=http://localhost:8081

# Request timeout in ms
VITE_API_TIMEOUT=15000

# (Optional) other future credentials / tokens (never commit real secrets)
# VITE_SENTRY_DSN=
# VITE_FEATURE_FLAG_ENABLED=true
